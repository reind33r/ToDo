<?php
try {
    $db = new PDO('mysql:host=localhost; dbname=DATABASE', 'USER', 'PASSWORD');
} catch(Exception $e) {
    exit('Erreur lors de la connexion à la base de données : '. $e->getMessage());
}
